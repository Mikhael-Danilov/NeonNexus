<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Black window background: the game UI is black, so cold start goes icon -> black -> game
         instead of flashing the stock light/dark DeviceDefault background first -->
    <style name="Theme.MyApplication" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
    </style>
</resources>
